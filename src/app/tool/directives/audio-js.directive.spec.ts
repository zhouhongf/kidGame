import { AudioJsDirective } from './audio-js.directive';

describe('AudioJsDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioJsDirective();
    expect(directive).toBeTruthy();
  });
});
