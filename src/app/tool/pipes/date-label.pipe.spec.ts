import { DateLabelPipe } from './date-label.pipe';

describe('DateLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new DateLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
